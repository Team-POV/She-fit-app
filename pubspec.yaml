name: she_fit_app
description: A new Flutter project focused on reproductive health and fitness management.
publish_to: "none" # Leave empty if you want to publish to a private repository.

version: 1.0.0+1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  cupertino_icons: ^1.0.8
  firebase_core: ^3.6.0
  firebase_auth: ^5.3.1
  cloud_firestore: ^5.4.4
  shared_preferences: ^2.3.2
  intl_utils: ^2.8.7
  intl: ^0.19.0
  table_calendar: ^3.1.2
  provider: ^6.1.2
  sensors_plus: ^6.1.0
  percent_indicator: ^4.2.3
  fl_chart: ^0.69.0
  url_launcher: ^6.3.1

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
